---
phase: 1
slug: list-navigation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-27
---

# Phase 1 — UI Design Contract: List & Navigation

> Visual and interaction contract for Phase 1. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (native SwiftUI) |
| Preset | not applicable |
| Component library | SwiftUI + AppKit (macOS 14+) |
| Icon library | SF Symbols |
| Font | System font (.system) — San Francisco |

Source: `.design-engineer/system.md`, `CategoryColors.swift`, existing tab views.

---

## Spacing Scale

Base unit: 8px. All values are multiples of 4.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, color strip width, dot indicators |
| sm | 8px | Intra-card element spacing, WeekStrip day cell vertical padding, card vertical padding |
| md | 16px | Card horizontal padding, section horizontal padding |
| lg | 24px | Section vertical padding, list outer padding |
| xl | 32px | — (not used in this phase) |
| 2xl | 48px | — (not used in this phase) |
| 3xl | 64px | — (not used in this phase) |

Exceptions:
- Color strip on card left edge: 3px wide (design system "timeline segments" token)
- Card corner radius: 10px (design system "cards/goal" token)
- Week navigation bar vertical padding: 8px top/bottom, 16px horizontal (matches CalendarTabView pattern)
- WeekStrip day cell corner radius: 8px (matches WeekStripView existing implementation)
- Card vertical padding: 8px top/bottom
- Gap between session cards: 8px

Source: `.design-engineer/system.md`, `CalendarTabView.swift`, `WeekStripView.swift`.

---

## Typography

All sizes use `.system` font (San Francisco). Weights: regular (400) and semibold (600) only.

| Role | Size | Weight | Line Height | Color Token | Usage |
|------|------|--------|-------------|-------------|-------|
| Small label | 10px | semibold (600) | 1.0 | `Theme.textQuaternary` | "MON", "TUE" day labels in WeekStrip (uppercase, 0.5px letter-spacing) and daily hour totals (e.g. "3.2h"), time range on session card, app name + duration in expanded detail |
| Body | 13px | regular (400) | 1.4 | `Theme.textSecondary` | Intention text on session card; muted placeholder when no intention |
| Heading | 13px | semibold (600) | 1.2 | `Theme.textPrimary` | Category name on session card (top line, left); duration badge right-aligned (e.g. "45m") |
| Day date number | 16px | regular (400) | 1.0 | `Theme.textPrimary` / `CategoryColors.accent` | Day-of-month number in WeekStrip |

Typography note: day labels and hour totals in WeekStrip both serve small auxiliary roles at the same 10px size — day labels use semibold 600 for uppercase abbreviations, hour totals use regular 400 for numeric readout. Duration badge on session cards uses 13px semibold (merged into the Heading role) to maintain the 2-weight constraint while preserving visual alignment with the category name on the same row.

Primary focal point: the session card list; within each card, the category name line (top-left, 13px semibold) is the primary visual anchor.

Source: `WeekStripView.swift` (sizes 10, 16 confirmed), `.design-engineer/system.md` (section label spec), `TodayTabView.swift` (14px body precedent — 13px used here for denser card rows).

---

## Color

| Role | Light | Dark | Usage |
|------|-------|------|-------|
| Dominant (60%) | `#f7f5f2` (`Theme.background`) | `#242220` | Window and scroll area background |
| Secondary (30%) | `#f2efec` (`Theme.backgroundSecondary`) | `#1e1c1a` | Session card fill |
| Accent (10%) | `#c06040` (`CategoryColors.accent`) | `#c06040` | Selected day in WeekStrip, "Today" nav button, today dot indicator, selected tab highlight, active tab bar icon |
| Category color | per-category (e.g. `#7b8db8` Coding) | per-category (e.g. `#6878a0`) | Left color strip on session card only |
| Border | `rgba(0,0,0,0.06)` (`Theme.border`) | `rgba(255,255,255,0.05)` | Card border stroke (0.5pt), section dividers |
| Destructive | N/A in Phase 1 | N/A in Phase 1 | Not used — edit/delete are Phase 2 |

Accent reserved for:
- Selected day cell background tint (`CategoryColors.accent.opacity(0.1)`) and text/icon in WeekStrip
- "Today" navigation button label
- Today dot indicator (4px circle) in WeekStrip
- Bottom tab bar icon + label for the active tab (`.sessions`)

Category colors are strictly for the left edge strip on session cards. They are never used as background fills or text colors.

Source: `CategoryColors.swift`, `.design-engineer/system.md`, `WeekStripView.swift`, `MainWindowView.swift`.

---

## Component Inventory

### SessionsTabView (new)

Top-level container view. Mirrors `CalendarTabView` structure without the timeline.

```
VStack(spacing: 0)
  WeekNavigationBar          ← existing pattern from CalendarTabView
  WeekStripView              ← reuse as-is, zero changes
  ScrollView(.vertical)
    VStack(spacing: 8)
      ForEach(sessions)
        SessionCardView      ← new
```

### WeekNavigationBar (inline in SessionsTabView)

Reuse the exact HStack pattern from `CalendarTabView` body:
- Left: `chevron.left` plain button — shifts week back — `.accessibilityLabel("Previous week")`
- Center: "Today" plain button in `CategoryColors.accent`
- Right: `chevron.right` plain button — shifts week forward — `.accessibilityLabel("Next week")`
- No calendar filter menu (that is Calendar-tab-specific)
- Horizontal padding: 16px, vertical padding: 8px

### WeekStripView (reused, unchanged)

Zero modifications. Pass `selectedDate`, `dailyTotals`, `onSelectDate` as already defined.

### SessionCardView (new)

Card anatomy (left to right, top to bottom):

```
HStack(spacing: 0)
  Rectangle (color strip)    3px wide, full card height, category color, cornerRadius on leading edge only
  VStack(spacing: 4)         horizontal padding: 16px left, 16px right; vertical padding: 8px
    HStack
      Circle (live dot)      4px, CategoryColors.accent, visible only for in-progress session
                             .accessibilityLabel("In progress")
      Text(category)         13px semibold, Theme.textPrimary
      Spacer
      Text(duration)         13px semibold, Theme.textPrimary  e.g. "45m"
    Text(intention)          13px regular, Theme.textSecondary (if nil: "No intention" in Theme.textQuaternary)
    Text(timeRange)          10px regular, Theme.textQuaternary  e.g. "09:15 – 10:00"
    if expanded:
      Divider                Theme.border, top margin 6px
      AppUsageListView
```

Card container:
- Background: `Theme.backgroundSecondary`
- Corner radius: 10px
- Border: 0.5pt stroke `Theme.border`
- No shadow (depth strategy: borders only per design system)
- `.contentShape(Rectangle())` for full hit area
- Tap gesture toggles `expandedSessionId` (accordion: sets to this id, or nil if already expanded)

### AppUsageListView (new, shown inside expanded card)

Visual treatment: simple vertical list. No mini bars (insufficient horizontal space in dense card). Chosen by Claude's discretion per CONTEXT.md D-05 and design system density principle ("a watch face you glance at").

```
VStack(spacing: 4)
  ForEach(appsUsed)
    HStack
      Text(appName)         10px regular, Theme.textSecondary
      Spacer
      Text(duration)        10px semibold, Theme.textTertiary  e.g. "45m"
```

Padding: 0px — inherits card's 16px horizontal padding. Top padding: 6px above first row (after divider).

### EmptyStateView (new, inline in SessionsTabView)

```
VStack(spacing: 8)
  Text("No sessions")        13px regular, Theme.textTertiary
```

Centered in the scroll area. No illustration, no action button — matches D-06.

---

## Interaction Contract

### Accordion Expansion (DETAIL-01, DETAIL-02, DETAIL-03)

State: `@State private var expandedSessionId: UUID?`

- Tapping a collapsed card: set `expandedSessionId = session.id`
- Tapping an already-expanded card: set `expandedSessionId = nil`
- Changing `selectedDate`: set `expandedSessionId = nil` (explicit `onChange`)
- Animation: `.animation(.easeInOut(duration: 0.2), value: expandedSessionId)` on the card's content height

Only one card expanded at a time. This is enforced by the single `UUID?` state — not a Set.

### Week Navigation (NAV-02)

- Chevron buttons: call `shiftWeek(-1)` / `shiftWeek(1)` — same pattern as CalendarTabView
- "Today" button: set `selectedDate = Date()`, reload week sessions
- Week boundary: no hard limit — allows browsing historical weeks indefinitely

### Day Selection (NAV-03)

- Tap any day cell in WeekStrip: `selectedDate = day`, triggers `loadWeekSessions()` via `.onChange`
- Selected day cell: accent tint background (`CategoryColors.accent.opacity(0.1)`), accent text
- Today dot: 4px filled circle, `CategoryColors.accent`, shown only when day is today

### Live Today Merge (LIST-03)

Computed property `selectedDaySessions` — identical pattern to `CalendarTabView.selectedDaySessions`:
- CloudKit sessions for the day, deduplicated against live IDs
- Append `sessionEngine.todaySessions`
- Append `sessionEngine.currentSession` if non-nil
- Sort by `startTime` ascending

### Tab Registration (NAV-01)

New `AppTab.sessions` case:
- Raw value: `"Sessions"`
- Icon: `"list.bullet.rectangle"` (SF Symbols — represents a structured list, distinct from calendar and clock)
- Position: between `.today` and `.calendar` in `AppTab.allCases` order

Tab bar renders `Sessions` between Today and Calendar.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Tab label | "Sessions" |
| Week navigation — today button | "Today" |
| Empty state | "No sessions" |
| No intention placeholder | "No intention" |
| In-progress session label | (no text label — indicated by terracotta dot only, with accessibility label "In progress") |
| Error state (load failure) | "Couldn't load sessions" |
| Error state subtext | "Pull to refresh or check your connection" |
| Section header (none declared) | No section headers in this phase |
| Chevron left accessibility label | "Previous week" |
| Chevron right accessibility label | "Next week" |

Notes:
- "No sessions" is the complete empty state — no subtext, no call to action (D-06: minimal, no illustrations or hints)
- "No intention" renders in `Theme.textQuaternary` (most muted text token) to signal it is a placeholder, not content
- Duration formatting: use shortest form — "45m" for under 1 hour, "1h 12m" for 1+ hours, "2h" if exactly on the hour
- Error subtext renders at 10px regular `Theme.textQuaternary`, below the primary error string

---

## Registry Safety

Not applicable. This project uses native SwiftUI with no third-party component registries. All components are either:
- Existing project components (WeekStripView, BackfillSheetView patterns)
- New SwiftUI views authored in this phase

No registry vetting required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
